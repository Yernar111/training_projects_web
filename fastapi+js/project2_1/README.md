# FastAPI + Javascript

The goal is to understand basic principles of REST API

Working with database server(PostgreSQL) using FastAPI. HTML and Javascript is provided for minimal user interface(for sending new data to database and get it from there). 

FastAPi concepts are commented in files