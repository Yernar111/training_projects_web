# Django + Angular

Angular is provided for minimal user interface and to send HTTP-requests to backend(Django)
Django receives HTTP-requests, then executes appropriate operations with database. There are 2 apps in django folder with the same logic: blog1(without DRF) and blog2(with DRF).